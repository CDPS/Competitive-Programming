#include <bits/stdc++.h>

using namespace std;

stack<int> a[25];

int n;

void moveOnto(int a, int b){

}

int main(){

    cin >> n;
    string line, c, a;
    int s1, s2;

    while(getline(cin,line) && line[0]!='q'){
        stringstream s(line);
        s >> c >> s1 >> a >> s2;
    }
    return 0;
}
