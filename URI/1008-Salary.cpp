#include <bits/stdc++.h>

using namespace std;

int main(){

     int a,b;
     double c;

    cin >>a;
    cin >> b;
    cin >> c;
     printf("NUMBER = %d\n",a);
     printf("SALARY = U$ %.2lf\n",c*( (double)b ) );
    return 0;
}
